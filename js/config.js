// Central settings. Edit these first.
export const CONFIG = {
  BOT_USERNAME: "BuzzwalletBot", // your bot's @username, without the @
  API_BASE: "",                    // backend URL, e.g. "https://api.example.com"
  USE_MOCK: true,                  // true = fake in-memory data; false = call the real backend
  MIN_WITHDRAW: 5,                 // USD
  REFERRAL_BONUS: 0.10             // USD per invited friend
};
